#include <cmath>
#include "ExampleMath.h"

namespace examplemath
{
    double root(double x)
    {
        return std::sqrt(x);
    }
    double square(double x)
    {
        return x * x;
    }
    double sin(double x)
    {
        return std::sin(x);
    }
}